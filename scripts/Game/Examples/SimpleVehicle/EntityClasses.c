// Entity Class aliases to satisfy engine requirements for SCR-derived script entities
// 实体类别名，满足引擎对 SCR 派生脚本实体的要求
// These are intentionally empty; they only establish the required Class types so the engine
// can recognize the script entity names used in the examples.
// 这些类刻意为空；仅建立所需类型以便引擎识别示例中使用的脚本实体名。

class SCR_SpawnAndBindVehicleDemoClass : SCR_BaseGameModeClass {}
class SCR_SimpleVehicleDemoClass : SCR_BaseGameModeClass {}
